
	npm install async-request --save
	
rule for sync and await: any request we want to use with await must be an asynchronous request

	npm install -g create-react-app
	
React is build on library
	
all components fall inside the root tag on demand

we should never do a circular reference


Rules
=========
1.All the tags should be closed
2. Instead of "class" "ClassName" should be used


Databinding : to bind data in react
	we use {}  to bind data
	
state should be declared in constructor and then update it using setstate method

